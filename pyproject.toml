[project]
name = "rtf-to-json"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "striprtf>=0.0.29",
    "pydantic>=2.0",
    "jsonschema>=4.0",
    "pytest>=7.0"
]

[project.scripts]
rtf-to-json = "rtf_to_json.__main__:main"
